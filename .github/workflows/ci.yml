name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11"]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        version: 1.7.1
        virtualenvs-create: true
        virtualenvs-in-project: true

    - name: Load cached venv
      id: cached-poetry-dependencies
      uses: actions/cache@v3
      with:
        path: .venv
        key: venv-${{ runner.os }}-${{ matrix.python-version }}-${{ hashFiles('**/poetry.lock') }}

    - name: Install dependencies
      if: steps.cached-poetry-dependencies.outputs.cache-hit != 'true'
      run: poetry install --no-interaction --no-root

    - name: Install project
      run: poetry install --no-interaction

    - name: Run tests
      run: |
        # For PR #47 (BS-8): Focus only on TemuScraperTool tests
        poetry run pytest tests/tools/scrapers/test_temu_scraper.py -v --cov=tools.scrapers.temu_scraper --cov-report=xml --cov-fail-under=50

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.xml
        fail_ci_if_error: true

  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"

    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        version: 1.7.1
        virtualenvs-create: true
        virtualenvs-in-project: true

    - name: Load cached venv
      id: cached-poetry-dependencies
      uses: actions/cache@v3
      with:
        path: .venv
        key: venv-${{ runner.os }}-lint-${{ hashFiles('**/poetry.lock') }}

    - name: Install dependencies
      if: steps.cached-poetry-dependencies.outputs.cache-hit != 'true'
      run: poetry install --no-interaction --no-root

    - name: Install project
      run: poetry install --no-interaction

    - name: Run black
      run: poetry run black tools/scrapers/temu_scraper.py tests/tools/scrapers/test_temu_scraper.py --check

    - name: Run isort
      run: poetry run isort tools/scrapers/temu_scraper.py tests/tools/scrapers/test_temu_scraper.py --check-only --diff

    - name: Run flake8
      run: poetry run flake8 tools/scrapers/temu_scraper.py tests/tools/scrapers/test_temu_scraper.py

    - name: Run mypy
      run: poetry run mypy tools/scrapers/temu_scraper.py

    - name: Run bandit
      run: poetry run bandit -r tools/scrapers/temu_scraper.py

    - name: Run interrogate
      run: poetry run interrogate -v tools/scrapers/temu_scraper.py
